<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Genesa.Scynapse.GrainDirectory.AdoNet</PackageId>
    <Title>Microsoft Scynapse ADO.NET Grain Directory Provider</Title>
    <Description>Microsoft Scynapse grain directory provider backed by ADO.NET</Description>
    <PackageTags>$(PackageTags) ADO.NET SQL</PackageTags>
    <TargetFrameworks>$(DefaultTargetFrameworks)</TargetFrameworks>
    <ScynapseBuildTimeCodeGen>true</ScynapseBuildTimeCodeGen>
    <VersionSuffix Condition="$(VersionSuffix) == ''">alpha.1</VersionSuffix>
    <Nullable>Enable</Nullable>
    <ImplicitUsings>Enable</ImplicitUsings>
    <NoWarn>CA2007</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <AssemblyName>Scynapse.GrainDirectory.AdoNet</AssemblyName>
    <RootNamespace>Scynapse.GrainDirectory.AdoNet</RootNamespace>
    <DefineConstants>$(DefineConstants);GRAINDIRECTORY_ADONET</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Tester.AdoNet" />
    <InternalsVisibleTo Include="Benchmarks.AdoNet" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="..\Shared\Storage\*.cs" LinkBase="Storage" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="$(SourceRoot)src\Scynapse.Runtime\Scynapse.Runtime.csproj" />
  </ItemGroup>

</Project>
