{
  "id": "TV-902",
  "title": "M1-S4 Strict Expired Assertion",
  "slice_profile": "M1-S4",
  "expect_conformance": "fail",
  "expected_error_ids": [
    "E3081_M1S4_PROOF_EXPIRED"
  ],
  "expected_error_contains": [
    "expired"
  ],
  "messages": [
    { "type": "HandshakeInit", "role": "requester" },
    { "type": "HandshakeChallenge", "role": "mediator" },
    { "type": "HandshakeProof", "role": "requester", "body": { "verification_mode": "strict", "proof_ref": "nid:m1s4.strict.expired", "strict_failure_mode": "expired" } }
  ],
  "expected_state_trace": [
    "MediatedHandshake",
    "Deny"
  ],
  "expected_outcome": {
    "success": false,
    "deny_code": "TrustInsufficient",
    "retryable": false
  },
  "assertions": [
    { "id": "A1", "kind": "state", "check": "final_state_equals", "value": "Deny" },
    { "id": "A2", "kind": "error", "check": "deny_code_equals", "value": "TrustInsufficient" }
  ]
}
