{
  "id": "TV-802",
  "title": "M1-S3 Strict Replay Detection",
  "slice_profile": "M1-S3",
  "expect_conformance": "fail",
  "expected_error_ids": [
    "E3072_M1S3_NONCE_REPLAY_DETECTED"
  ],
  "expected_error_contains": [
    "replay"
  ],
  "preconditions": [
    "strict mode replay probe is requested"
  ],
  "messages": [
    { "type": "HandshakeInit", "role": "requester" },
    { "type": "HandshakeChallenge", "role": "mediator" },
    { "type": "HandshakeProof", "role": "requester", "body": { "verification_mode": "strict", "proof_ref": "nid:m1s3.strict.replay", "replay_probe": true } }
  ],
  "expected_state_trace": [
    "MediatedHandshake",
    "Deny"
  ],
  "expected_outcome": {
    "success": false,
    "deny_code": "TrustInsufficient",
    "retryable": false
  },
  "assertions": [
    { "id": "A1", "kind": "state", "check": "final_state_equals", "value": "Deny" },
    { "id": "A2", "kind": "error", "check": "deny_code_equals", "value": "TrustInsufficient" }
  ]
}
