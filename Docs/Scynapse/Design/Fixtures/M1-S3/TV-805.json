{
  "id": "TV-805",
  "title": "M1-S3 Mock Replay Detection",
  "slice_profile": "M1-S3",
  "expect_conformance": "fail",
  "expected_error_ids": [
    "E3072_M1S3_NONCE_REPLAY_DETECTED"
  ],
  "expected_error_contains": [
    "nonce replay"
  ],
  "preconditions": [
    "mock mode replay flag is asserted"
  ],
  "messages": [
    { "type": "HandshakeInit", "role": "requester" },
    { "type": "HandshakeChallenge", "role": "mediator" },
    { "type": "HandshakeProof", "role": "requester", "body": { "verification_mode": "mock", "mock_signature_valid": true, "mock_replay_detected": true } }
  ],
  "expected_state_trace": [
    "MediatedHandshake",
    "Deny"
  ],
  "expected_outcome": {
    "success": false,
    "deny_code": "TrustInsufficient",
    "retryable": false
  },
  "assertions": [
    { "id": "A1", "kind": "state", "check": "final_state_equals", "value": "Deny" },
    { "id": "A2", "kind": "error", "check": "deny_code_equals", "value": "TrustInsufficient" }
  ]
}
